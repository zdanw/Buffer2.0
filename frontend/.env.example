# =============================================================================
# 前端环境变量说明
# =============================================================================
#
# 浏览器始终请求同源相对路径 /v1（axiosInstance），由 Vercel 代理到 HF Space。
# 切勿在 Vercel 设置任何会打进前端包的 VITE_* 后端地址，尤其不要：
#   VITE_BACKEND_URL=http://xxx.hf.space
# 否则 HTTPS 站点会触发 Mixed Content 被浏览器拦截。
#
# 本地开发：Vite 在 vite.config.ts 把 /v1 代理到 http://localhost:8080，无需再配 VITE_*。
#
# -----------------------------------------------------------------------------
# Vercel 项目 Environment Variables（仅服务端 / 构建，不要加 VITE_BACKEND_URL）
# -----------------------------------------------------------------------------
# HF_SPACE_HOST=zongsechenai-bebcare-buffer.hf.space
# ALLOWED_ORIGINS=https://buffrer2-0.vercel.app
