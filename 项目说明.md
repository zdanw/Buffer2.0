# PulseForge 项目说明


| 项目名称 | PulseForge（全自动社媒内容锻造系统）  |
| ---- | ---------------------------------- |
| 版本   | 2.0.0                              |
| 文档日期 | 2026-08-05                         |
| 文档性质 | 项目说明（系统是什么、怎么用、怎么部署与维护）            |
| 配套文档 | 《项目书》侧重建设目标、架构决策与里程碑；本文侧重交付说明与日常使用 |


---

## 1. 文档目的

本文面向：

- **新成员上手**：快速理解系统边界、目录职责与主流程
- **运营 / 内容**：知道后台各页面做什么、自动与人工模式如何配合
- **开发 / 运维**：本地联调、环境变量、部署副本同步与排障入口

更完整的立项目标、成功标准、风险表与里程碑见根目录 `[项目书.md](项目书.md)`。仓库操作级说明见 `[README.md](README.md)`。

---

## 2. 系统是什么

### 2.1 一句话

**PulseForge** 支持多品牌套件：品牌定义语音与内容规则，产品继承品牌配置。根据产品资产与可配置提示词维度，自动生成社媒文案与产品图；经去重后，可选择直接通过 Buffer 发布，或进入人工审核后再发布。

### 2.2 业务闭环（运营视角）

```
产品与参考图  →  提示词维度  →  定时/手动生成  →  去重  →  CDN 落盘
                                                              │
                                              ┌───────────────┴───────────────┐
                                              ▼                               ▼
                                         自动模式                          人工模式
                                      Buffer 直发                    待发页挑选后发布
```



### 2.3 不做什么

- 不替代各社媒原生后台的深度运营分析；发布与排队以 Buffer 为准
- 默认不启用 CLIP 重模型；开启需额外依赖与权重

---



## 3. 角色与日常用法


| 角色      | 典型操作                                   |
| ------- | -------------------------------------- |
| 运营 / 市场 | 配任务 Cron 与平台；看日历；人工模式下在「待发」选图选文并发布     |
| 内容策划    | 维护维度（场景、光线、构图等）、产品卖点与品牌相关配置            |
| 管理员     | 用户权限；图像 Provider / 模型配置与连通性测试          |
| 开发      | 只改 `backend/`（及 `frontend/`），同步部署副本后发布 |




### 3.1 推荐上手路径（5 分钟首帖）

1. 本地启动后端 `:8080`、前端 `:5174`（见第 8 节）
2. 使用 `ADMIN_*` 账号登录 — 首次登录会弹出入门向导（可跳过）
3. **品牌**：创建品牌套件，或使用系统 **Generic** / **Bebcare** 品牌
4. **产品**：添加产品并上传至少一张参考图（产品图 / 场景图）
5. **Studio**：选择品牌与产品，生成文案与图像预览
6. **自动化**：配置 Cron、目标范围、`auto` 或 `manual`
7. 人工模式到 **审核** 队列；自动模式在日历中核对结果

> **视觉风格**（可选）：默认在首次启动时自动加载通用预设，全站共享，无需手动初始化。管理员可在「视觉风格」页导入母婴预设或重置；Bebcare 生产环境可设 `SEED_BABY_DIMENSIONS=true`。

### 3.2 经典上手路径

1. 本地启动后端 `:8080`、前端 `:5174`（见第 8 节）
2. 使用 `ADMIN_*` 账号登录
3. **产品**：建产品并上传参考图（产品图 / 场景图）
4. **Studio**：对单个产品试生成，确认文案与出图质量
5. **自动化**：配置 Cron、目标范围、出图/文案数、`auto` 或 `manual`
6. 人工模式到 **审核** 审核；自动模式在执行记录 / 日历中核对结果

---



## 4. 系统架构说明



### 4.1 逻辑架构

```
┌─────────────────┐         HTTPS/JWT          ┌──────────────────────────────┐
│  Frontend       │ ◄─────────────────────────► │  Backend (FastAPI)            │
│  React + Vite   │                             │  /v1/* 业务 API               │
│  (Vercel)       │                             │  APScheduler 定时任务         │
└─────────────────┘                             └──────────────┬───────────────┘
                                                               │
        ┌──────────────────┬───────────────────┬───────────────┼───────────────┐
        ▼                  ▼                   ▼               ▼               ▼
   DeepSeek/百炼        豆包/多 Provider      Buffer API     GitHub 图床     Chroma
   (文案/图像Prompt)    (图像生成)            (社媒发布)     (CDN URL)       (向量/元数据)
                                      │
                                      ▼
                         Supabase Postgres（生产）/ SQLite（本地）
```



### 4.2 仓库目录职责


| 路径                              | 说明                             |
| ------------------------------- | ------------------------------ |
| `backend/`                      | **唯一业务主源码**（API、模型、迁移、测试）      |
| `frontend/`                     | 管理后台                           |
| `hf-space/`                     | Hugging Face Space 部署副本（由脚本同步） |
| `space4/`                       | 魔搭创空间部署副本（同上）                  |
| `scripts/sync_deploy_copies.py` | 同步 / `--check` 漂移检查            |
| 根目录 `Dockerfile` + `README.md`  | HF Space Docker SDK 入口         |
| `项目书.md` / 本文                   | 规划与说明文档                        |


**硬性约定**：改业务逻辑只动 `backend/`（前端改 `frontend/`），部署前执行同步脚本；CI 会对副本做 `--check`。

### 4.3 生成流水线说明


| 步骤              | 模块                     | 说明                                           |
| --------------- | ---------------------- | -------------------------------------------- |
| 组 Prompt        | `prompt_builder`       | 平台语气、叙事视角、写作风格、维度、负向提示                       |
| 写文案 / 图像 Prompt | `generator` + DeepSeek | 英文社媒文案 + 中文出图提示；可选多模态「视觉写 Prompt」            |
| 出图              | `providers`            | 豆包等可配置 Provider + 参考图                        |
| 去重              | `dedup`                | pHash（图）、MinHash（文）；可选 CLIP                  |
| 持久化             | GitHub 图床工具            | 得到 CDN URL                                   |
| 发布              | `publisher` / 草稿表      | `auto` → Buffer；`manual` → `ManualTaskDraft` |


---



## 5. 功能模块说明



### 5.1 管理台页面


| 路由              | 模块   | 说明                        |
| --------------- | ---- | ------------------------- |
| `/login`        | 登录   | JWT 登录与刷新                 |
| `/assets`       | 资产管理 | 产品 CRUD、参考图上传             |
| `/dimensions`   | 维度管理 | 提示词维度、产品类型兼容              |
| `/tasks`        | 任务配置 | Cron、目标、平台、数量、模式、Provider |
| `/pending`      | 待发审核 | 人工草稿：选图、选文、发布或丢弃          |
| `/preview`      | 内容预览 | 按产品/平台试生成                 |
| `/calendar`     | 发布日历 | 执行与发布时间线                  |
| `/image-models` | 图像模型 | Provider CRUD、测连通（管理员）    |
| `/users`        | 用户管理 | 用户增删改、管理员标记（管理员）          |




### 5.2 后端 API（前缀 `/v1`）


| 路由组                  | 能力                        |
| -------------------- | ------------------------- |
| `/auth`              | 登录、刷新、当前用户、用户 CRUD        |
| `/products`          | 产品与分类、图片上传/列表/删除          |
| `/tasks`             | 定时任务、草稿、CDN 重传、发布/丢弃、执行记录 |
| `/generate`          | 综合生成、仅文案、仅出图、异步状态         |
| `/publish`           | 发起发布、查询状态                 |
| `/prompt-dimensions` | 维度 CRUD、初始化、兼容、产品绑定       |
| `/image-providers`   | Provider CRUD、模型列表、测试     |


探活：`GET /`、`GET /health`（无需登录）。完整契约：服务启动后 `/docs`、`/redoc`。

### 5.3 核心子系统（实现说明）



#### Prompt 引擎（`prompt_builder`）

- 平台风格：Instagram / TikTok / Facebook 等语气、长度、emoji 比例
- 叙事视角与写作风格可组合（如种草、理性分析、温情故事）
- 硬性负向提示（水印、乱字、商标变形等）+ 多样性建议
- 维度入库管理，并按产品类型做兼容策略



#### 内容生成器（`generator`）

- DeepSeek Chat Completions；`DEEPSEEK_API_URL` 可填百炼 OpenAI 兼容 base，代码自动补全 `/chat/completions`
- 异步生成 + 重试；调度侧有同步封装
- 出图结果上传 GitHub，写入 CDN URL



#### 去重（`dedup`）

- **pHash**：近似重复图
- **MinHash/LSH**：文案近重复
- **CLIP**：`ENABLE_CLIP=true` 时启用（需 `requirements-clip.txt` 与权重）



#### 调度（`scheduler`）

- Cron；同任务不重叠（`max_instances` + `coalesce`）
- `MAX_CONCURRENT_JOBS` 全局限流，适配 HF Space
- 执行轨迹写入 `TaskExecution`



#### 发布（`publisher`）

- Buffer GraphQL，带重试
- 平台映射含 IG / FB / TikTok / X / LinkedIn / Pinterest / YouTube / Threads 等



#### 知识库（`knowledge_base`）

- Chroma 存图像元数据与向量检索入口；默认不加载重模型

---



## 6. 数据实体说明


| 实体                         | 含义                        |
| -------------------------- | ------------------------- |
| `User`                     | 账号、密码哈希、是否管理员             |
| `Product` / `ProductImage` | 产品与参考图（CDN、pHash、类型）      |
| `PromptDimension` 及相关      | 维度、产品类型、兼容、产品维度关联         |
| `ScheduledTask`            | Cron、范围、平台、数量、模式、Provider |
| `TaskExecution`            | 单次调度轨迹（状态、文案、维度、错误等）      |
| `ManualTaskDraft`          | 人工待审草稿（多图多文、选中项、发布状态）     |
| `GenerateTask`             | 异步生成任务状态                  |
| `ImageProvider`            | 图像服务商配置（密钥加密存储）           |
| `Publish*` / `Log`         | 发布记录与系统日志                 |


迁移：Alembic（`backend/migrations/`）。`AUTO_MIGRATE=true` 时启动自动 `upgrade`。拉代码后的操作步骤见 **§8.2**。

说明：`TaskExecution` 等字段随版本演进（例如与产品关联的执行记录），以当前 `models` 与迁移文件为准。

---



## 7. 安全与配置要点

1. **鉴权**：业务 API 依赖 JWT；探活除外。
2. **CORS**：`ALLOWED_ORIGINS` 白名单；禁止 `*`。
3. **密钥**：`SECRET_KEY` 用于 JWT 与 Provider Key 加密；仅环境变量 / Space Secrets。
4. **生产库**：`APP_ENV=production` 禁止 SQLite，须 Supabase Postgres（Session `5432` + `sslmode=require`）。
5. **前端代理**：浏览器只打同源 `/v1`；Vercel 勿把 `http://*.hf.space` 打进前端包（Mixed Content）。

关键环境变量摘要：`APP_ENV`、`DATABASE_URL`、`DEEPSEEK_*`、`DOUBAO_*`、`BUFFER_*`、`GITHUB_*`、`SECRET_KEY`、`ADMIN_PASSWORD`、`ALLOWED_ORIGINS`，以及 `MAX_CONCURRENT_JOBS`、`DB_POOL_SIZE`、`ENABLE_CLIP` 等。完整模板见 `backend/.env.example`。

---



## 8. 部署与运维说明



### 8.1 本地开发

```bash
# 后端
cd backend
cp .env.example .env
pip install -r requirements.txt
uvicorn bebcare.main:app --host 0.0.0.0 --port 8080 --reload

# 前端（另开终端）
cd frontend
npm ci
npm run dev    # :5174，/v1 → :8080
```

分目录说明：`backend/README.md`、`frontend/README.md`。

### 8.2 数据库迁移（其他开发者必读）

拉取含新 `backend/migrations/` 的代码后，**必须先升级 schema 再使用新功能**（品牌套件、Logo、视觉风格 ID 等）。完整说明见 `[backend/README.md](backend/README.md)` 数据库迁移一节。

#### 本地开发（SQLite）

```bash
cd backend
pip install -r requirements.txt          # 依赖有变时
cp .env.example .env                     # 首次
# 方式 A（默认）：AUTO_MIGRATE=true，直接重启后端
uvicorn bebcare.main:app --host 0.0.0.0 --port 8080 --reload

# 方式 B：先手动迁移
python -m alembic upgrade head
python -m alembic current                # 应显示 016_dimension_scope_unique 或 (head)
uvicorn bebcare.main:app --host 0.0.0.0 --port 8080 --reload
```

- 全新库：迁移建表 + 种子数据；视觉风格默认自动加载 **general** 预设
- 需母婴预设：`.env` 设 `SEED_BABY_DIMENSIONS=true` 后重启
- 迁移 **016** 会清理重复的 `prompt_dimensions` 行；有自定义数据请先备份 `bebcare.db`

#### 生产部署（HF Space + Supabase）

```bash
# 1. 同步部署副本后推送 / 重建 Space
python scripts/sync_deploy_copies.py

# 2. Space Secrets：APP_ENV=production、DATABASE_URL（Supabase Session 5432）
#    AUTO_MIGRATE=true（单实例推荐）或 false（须先手动迁移）

# 3a. AUTO_MIGRATE=true：部署新版本后重启 Space（日志见 Running database migrations）

# 3b. 手动迁移（AUTO_MIGRATE=false 或多实例）— 在可连生产库的环境执行：
cd backend
export DATABASE_URL="postgresql://..."    # PowerShell: $env:DATABASE_URL="..."
export APP_ENV=production
python -m alembic upgrade head
python -m alembic current
```

- 大版本升级前在 Supabase 做备份
- Bebcare 生产：`SEED_BABY_DIMENSIONS=true` 可在启动时追加母婴视觉预设
- 禁止生产使用 SQLite

### 8.3 生产拓扑


| 组件  | 建议                                        |
| --- | ----------------------------------------- |
| 后端  | HF Space（Docker，7860）                     |
| 前端  | Vercel，`HF_SPACE_HOST` + 后端白名单            |
| 数据库 | Supabase Postgres                         |
| 密钥  | Space Secrets（参考 `hf-space/.env.example`） |


```bash
python scripts/sync_deploy_copies.py          # 同步 hf-space/、space4/
python scripts/sync_deploy_copies.py --check  # CI 漂移门禁
```

HF 只认**仓库根目录**的 `Dockerfile` 与带 `sdk: docker` 的 README。细节：`hf-space/README.md`、`space4/README.md`。

### 8.4 CI

`.github/workflows/ci.yml`：后端 `pytest`、前端 `npm run build`、副本 `--check`。

### 8.5 常见注意


| 现象 / 风险             | 处理建议                                                       |
| ------------------- | ---------------------------------------------------------- |
| Space 资源紧、任务堆叠      | `MAX_CONCURRENT_JOBS=1`，错开 Cron                            |
| Supabase 连不上 / 连接耗尽 | 检查 Session 串与 `sslmode`；减小 `DB_POOL_SIZE`                  |
| 副本与 backend 不一致     | 跑同步脚本；看 CI `--check`                                       |
| 生成失败                | 查 `TaskExecution` / Space Logs；核对 DeepSeek、豆包、GitHub Token |
| 发布失败                | 查 Buffer Token 与平台账号绑定；看发布记录                               |


---



## 9. 代码组织结构（简图）

```
Bebcare_Buffer2.0/
├── backend/bebcare/
│   ├── api/              # HTTP 路由
│   ├── generator/        # 文案与出图
│   ├── prompt_builder/   # Prompt 引擎
│   ├── dedup/            # 去重
│   ├── publisher/        # Buffer
│   ├── scheduler/        # 定时任务
│   ├── providers/        # 图像 Provider
│   ├── knowledge_base/   # Chroma
│   ├── models/ schemas/ services/
│   └── main.py
├── frontend/src/         # 管理台页面与 API 客户端
├── hf-space/             # HF 部署副本
├── space4/               # 魔搭部署副本
├── scripts/              # 同步工具
├── README.md             # 仓库详细说明（安装/配置/结构）
├── 项目书.md             # 建设目标、里程碑、风险
└── 项目说明.md           # 本文
```

---



## 10. 术语表


| 术语            | 含义                                         |
| ------------- | ------------------------------------------ |
| 维度（Dimension） | 构成 Prompt 的可控选项（场景、光线、构图等）                 |
| 参考图           | 产品图或场景图，约束图像模型                             |
| 草稿（Draft）     | 人工模式下待挑选的图文集合                              |
| Provider      | 可插拔图像生成服务配置                                |
| Buffer        | 第三方社媒排队与发布平台                               |
| 部署副本          | `hf-space/`、`space4/` 中与 backend 对应的可部署代码树 |


---



## 11. 相关文档索引


| 文档                                         | 用途                    |
| ------------------------------------------ | --------------------- |
| `[项目书.md](项目书.md)`                         | 概述、目标、架构决策、里程碑、风险     |
| `[README.md](README.md)`                   | 特性、安装、环境变量、使用与部署细节    |
| `[backend/README.md](backend/README.md)`   | 后端本地开发、迁移、API 摘要      |
| `[frontend/README.md](frontend/README.md)` | 前端本地开发、Vercel 注意点     |
| `[hf-space/README.md](hf-space/README.md)` | Hugging Face Space 部署 |
| `[space4/README.md](space4/README.md)`     | 魔搭创空间部署               |
| `backend/.env.example` 等                   | 环境变量模板                |


---

*本文与《项目书》配套：规划看项目书，上手与运维看本文与 README。随版本迭代请同步更新版本号与索引路径。*