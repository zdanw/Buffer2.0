[pytest]
testpaths = tests/api tests/unit
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    apscheduler_job_runtime: requires a started APScheduler so Job.next_run_time exists (TestClient skips FastAPI lifespan)
